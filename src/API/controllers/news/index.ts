
import { Request, Response } from "express";

export const getNews = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const getNew = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const postNew = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const deleteNew = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const changeNew = (req: Request, res: Response) => {
  res.status(200).send("Users");
};