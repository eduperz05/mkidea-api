
import { Request, Response } from "express";

export const getTeams = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const getTeam = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const postTeam = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const deleteTeam = (req: Request, res: Response) => {
  res.status(200).send("Users");
};

export const changeTeam = (req: Request, res: Response) => {
  res.status(200).send("Users");
};